import React from 'react';
import {FormattedMessage} from 'react-intl';

export class PageCatalog extends React.PureComponent {
    render() {
        return (
            <FormattedMessage id={'home'} />
        );
    }
}
