import * as actions from './actions';

const initialState = {

};

export function purchaserReducer(state = initialState, action) {

    switch (action.type) {

        default:
            return state;
    }
}
