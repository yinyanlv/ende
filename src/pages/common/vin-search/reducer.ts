import * as actions from './actions';

const initialState = {};

export function vinSearchReducer(state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
}
